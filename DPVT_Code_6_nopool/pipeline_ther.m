

path_cdnet = '~/dataset/dataset2014/';
video_path = 'thermal/corridor/';
%expLine(video_path,path_cdnet);

video_path = 'thermal/diningRoom/';
expLine(video_path,path_cdnet);

video_path = 'thermal/lakeSide/';
expLine(video_path,path_cdnet);

video_path = 'thermal/library/';
expLine(video_path,path_cdnet);

video_path = 'thermal/park/';
expLine(video_path,path_cdnet);
